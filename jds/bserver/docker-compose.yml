services:
  bserver:
    image: bserver:${VERSION}
    container_name: bserver
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ./luban:/bserver/config/luban
      - ./BattleSimulator:/bserver/BattleSimulator
    networks:
      - bserver
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8080/api/battle/simulator"]
      interval: 60s
      retries: 3
      timeout: 10s
      start_period: 30s

networks:
  bserver:
    driver: bridge